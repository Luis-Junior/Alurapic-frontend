/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterByDescription.pipe.tsPipe } from './filter-by-description.pipe.ts.pipe';

describe('Pipe: FilterByDescription.pipe.tse', () => {
  it('create an instance', () => {
    let pipe = new FilterByDescription.pipe.tsPipe();
    expect(pipe).toBeTruthy();
  });
});
